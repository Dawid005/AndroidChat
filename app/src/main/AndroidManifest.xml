<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.konspekt9_3">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Konspekt9_3">
        <activity android:name=".DeleteCommentActivity"
            android:exported="false"/>
        <activity
            android:name=".EditComment"
            android:exported="false" />
        <activity
            android:name=".CreateNewComment"
            android:exported="false" />
        <activity
            android:name=".GalleryAction"
            android:exported="false" />
        <activity
            android:name=".HomeAction"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Konspekt9_3.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>